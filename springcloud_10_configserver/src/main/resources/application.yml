server:
  port: 8848
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      server:
        git:
          uri: https://gitee.com/clarence_x/configs.git  # 远程仓库地址
          default-label: master # 远程仓库分支
